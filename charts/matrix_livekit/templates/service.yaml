apiVersion: v1
kind: Service
metadata:
  name: {{ include "matrix-livekit.fullname" . }}
  labels:
    {{- include "matrix-livekit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "matrix-livekit.selectorLabels" . | nindent 4 }} 