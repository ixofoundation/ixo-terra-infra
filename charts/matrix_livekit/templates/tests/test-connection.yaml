apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "matrix-livekit.fullname" . }}-test-connection"
  labels:
    {{- include "matrix-livekit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "matrix-livekit.fullname" . }}:{{ .Values.service.port }}/healthz']
  {{- with .Values.nodeSelector }}
  nodeSelector:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.tolerations }}
  tolerations:
    {{- toYaml . | nindent 4 }}
  {{- end }} 