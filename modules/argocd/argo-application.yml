apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ${name}
  namespace: ${argo_namespace}
spec:
  destination:
    namespace: ${namespace}
    server: https://kubernetes.default.svc
  project: default
  source:
    helm:
      valueFiles:
        - values.yaml
    path: charts/devnet/${namespace}
    repoURL: ${repository}
    targetRevision: HEAD
  syncPolicy:
    syncOptions:
      - CreateNamespace=true