apiVersion: monitoring.grafana.com/v1alpha1
kind: PodLogs
metadata:
  name: ${name}-logs
  namespace: ${namespace}
  labels:
    instance: ${name}-logs
spec:
  pipelineStages:
    - cri: {}
  relabelings:
    - action: replace
      sourceLabels:
        - __meta_kubernetes_pod_node_name
      targetLabel: __host__
    # Keep a small allowlist of stable Kubernetes identity labels for Loki filtering.
    # Avoid mapping *all* pod labels (high cardinality; trips max_label_names_per_series).
    - action: labelmap
      regex: __meta_kubernetes_pod_label_(app_kubernetes_io_name|app_kubernetes_io_instance|app_kubernetes_io_component|app_kubernetes_io_part_of)
    - action: replace
      replacement: $1
      separator: '-'
      sourceLabels:
        - __meta_kubernetes_pod_label_app_kubernetes_io_name
        - __meta_kubernetes_pod_label_app_kubernetes_io_component
      targetLabel: __service__
    - action: replace
      replacement: $1
      separator: /
      sourceLabels:
        - __meta_kubernetes_namespace
        - __service__
      targetLabel: job
    - action: replace
      sourceLabels:
        - __meta_kubernetes_pod_container_name
      targetLabel: container
    - action: replace
      sourceLabels:
        - __meta_kubernetes_pod_label_app_kubernetes_io_name
      targetLabel: name
    - action: replace
      replacement: ${name}
      targetLabel: cluster
  namespaceSelector:
    matchNames:
      - ${indent(6, join("\n- ", matchNamespaces))}
  selector:
    matchLabels:
      app.kubernetes.io/part-of: ${name}
