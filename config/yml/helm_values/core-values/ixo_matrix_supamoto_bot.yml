image:
  pullPolicy: Always
ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
  tls:
    - hosts:
        - ${host}
      secretName: ixo-matrix-supamoto-bot-tls
  hosts:
    - host: ${host}
      paths:
        - path: /
          pathType: Prefix  
persistentVolume:
  storageClass: ${storage_class}
  size: ${storage_size}
service:
  port: 8084
env:
  - name: "PORT"
    value: "8083"
resources:
  requests:
    memory: 100Mi
    cpu: "40m"
  limits:
    memory: 650Mi
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 1
  targetMemoryUtilizationPercentage: "30"
backup:
  resources:
    requests:
      cpu: 10m
      memory: 128Mi
    limits:
      memory: 256Mi
  gcs:
    bucket: ${gcs_bucket}